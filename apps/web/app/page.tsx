/**
 * The front page is the planet itself. The map — mounted once in the root
 * layout — is the entire surface; this route adds nothing on top of it.
 * The feed, the civilisations and the chronicle open as overlays from the
 * map's own chrome.
 */

export default function Page() {
  return null;
}
